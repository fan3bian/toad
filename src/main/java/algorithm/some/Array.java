package algorithm.some;

public class Array {
    /**
     * java 里数组移指定元素的实现方式是通过数组copy的方式实现
     * @param arr
     * @param element
     */
    public void removeElement(int arr[], int element) {
        for (int i = 0; i < arr.length; i++) {
            if (element == arr[i]) {

            }
        }

    }

}
